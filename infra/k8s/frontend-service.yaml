apiVersion: v1
kind: Service
metadata:
  name: aiops-frontend
spec:
  type: ClusterIP
  selector:
    app: aiops-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
apiVersion: v1
kind: Service
metadata:
  name: aiops-frontend
spec:
  type: ClusterIP
  selector:
    app: aiops-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
